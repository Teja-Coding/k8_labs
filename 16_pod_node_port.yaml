apiVersion: v1
kind: Pod
metadata:
  name: nginx-np
  labels:
    project: roboshop
    component: frontend
    environement: dev
    purpose: node-port
spec:
  containers:
  - name: my-container-nginx
    image: nginx
    ports:
    - containerPort: 80 