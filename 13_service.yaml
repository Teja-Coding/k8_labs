apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    project: roboshop
    component: frontend
    environement: dev
  ports:
    - protocol: TCP
      port: 80  
      targetPort: 80